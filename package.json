{
  "name": "agente-qa-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "core",
    "cli"
  ],
  "scripts": {
    "test": "vitest run",
    "build": "npm run build --workspace=core && npm run build --workspace=cli"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
